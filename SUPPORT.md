# Support

For integration issues, include:

- QuantLib version.
- Operating system and compiler.
- CMake configure command.
- Provider status from `quantlib-inspect --inventory`.
- Production status from `quantlib-inspect --production-ready`.

Do not include private keys, vault passphrases, shared secrets, raw vault files, or production audit logs in support requests.
